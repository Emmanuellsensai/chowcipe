VITE_PEXELS_API_KEY=your_pexels_api_key_here

# Server-side only. Used by the /api/recipes serverless proxy.
# Never prefix this with VITE_ — that would expose it to the browser bundle.
GEMINI_API_KEY=your_gemini_api_key_here
